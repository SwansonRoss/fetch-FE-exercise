import Warning from "./Warning"
const Header = (props) => {
    return(
        <header>
            <h1>Enter your contact info</h1>
            <Warning />
        </header>
    )
}
export default Header;